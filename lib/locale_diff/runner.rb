require 'pry'

module LocaleDiff
  class Runner

    def self.run!
      # start at the specified locale folder and make a record out of the immediate .yml files

      # then loop through every sub directory and do the same thing
    end

  end
end